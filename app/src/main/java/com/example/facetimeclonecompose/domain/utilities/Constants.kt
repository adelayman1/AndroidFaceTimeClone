package com.example.facetimeclonecompose.domain.utilities

object Constants {
    const val PASSWORD_PATTERN = "^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[^A-Za-z0-9]).{8,}$"
    const val MINIMUM_USER_NAME_LENGTH = 4
    const val LINK_ROOM_TYPE = "link"
}